<%= render partial: 'welcome/search'%>
<%@results.each do |result|%>
  <%=render_piece(result)%>
  <%=render partial: 'mixes/form', locals: {user: current_user, media: result} if current_user%>
<%end%>